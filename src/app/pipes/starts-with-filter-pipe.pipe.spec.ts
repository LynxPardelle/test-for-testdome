import { StartsWithFilterPipePipe } from './starts-with-filter-pipe.pipe';

describe('StartsWithFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StartsWithFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
